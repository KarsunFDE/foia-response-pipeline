# foia-response-pipeline — ai-orchestrator deps
#
# W4 modernization (authorized through end of W4):
#   - Item 5 CLOSED — migrated to LangChain v1.0 (langchain-classic split out;
#     LLMChain removed; legacy_chain.py deleted). Agentic FOIA triage workflow
#     (ADR 0005) is built on LangGraph with a MongoDB checkpointer for HITL
#     pause/resume (REQ-AGT-3).
#   - Item 7 CLOSED — pinecone-client removed (was never imported; Atlas Vector
#     Search via langchain-mongodb is the vector path, ADR 0003/0004).

fastapi==0.115.0
uvicorn[standard]==0.32.0

# LangChain v1.0 stack (Chain class removed; create_agent / LangGraph idiom).
langchain==1.3.9
langchain-core==1.4.7
langchain-aws==1.5.1
langchain-mongodb==0.11.0

# Agentic workflow + HITL persistence (ADR 0005).
langgraph==1.2.5
langgraph-checkpoint-mongodb==0.4.0

# Bedrock / AWS (boto3 floor raised by langchain-aws 1.5.1).
boto3==1.42.42

# Datastores / RAG (pymongo bounded by langgraph-checkpoint-mongodb 0.4.0: >=4.12,<4.17).
pymongo==4.16.0

# Schemas (floor raised by langchain-aws 1.5.1: >=2.10.6).
pydantic==2.10.6

# Form upload support for future endpoints
python-multipart==0.0.12
